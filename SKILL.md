---
name: naming-convention_diy
description: 统一命名方案的落地与校验。为任何对象命名前先经澄清（交付对象 → 领域前缀，产物用途 → 用途中缀，再定主题与版本状态），产出三段式 <领域>-<用途>-<主题>-<版本/状态> 名称；7 个领域前缀 + 9 个用途中缀 + 版本状态后缀；自建技能目录名以 _diy 结尾以便与上游技能区分。提供前缀选型、命名澄清、命名校验、存量审计。触发词：命名规范、命名方案、起名、给文件命名、目录命名、技能命名、命名审计、命名校验。
agent_created: true
---

# naming-convention_diy

把《WorkBuddy 工作区命名现状分析与前缀分类方案 v1》落地为可执行的命名规范。

**命名模板**

```
<领域>-<用途>-<主题>-<版本/状态>.<扩展名>

med-761tel-ia-rev-v2.md             干细胞管线尽调报告 第2版
res-chictr-stroke-lit-final.md      ChiCTR 检索清单 定稿
grant-qilu-2026-94-dsgn-wip.md      齐鲁专项技术路线 草稿
ai-dsh-plugin-survey-doc-final.md   dsh 插件调研 正式稿
ops-call-transcript-raw.md          通话转录 原始素材
```

**唯一的 `_diy` 规则**：**自建技能的目录名以 `_diy` 结尾**，用途是把它与上游/第三方技能区分开。`_diy` **不用于**普通文件、普通目录或代码标识符。

---

## 一、适用范围

### 1.1 规范适用（会被校验）

| 对象 | 适用规则 | 说明 |
|---|---|---|
| 自产交付文件（md/docx/pptx/xlsx/html） | N1–N6 | 命名的最大受益面 |
| 自产脚本与工具（py/mjs/ps1/bat） | N1–N3、N6 | 重点防"裸版号" |
| 自建目录 | N1–N6 | 项目目录按 `<领域>-<主题>-<YYYYMMDD>` |
| **自建技能目录** | N1–N9 | 唯一需要 `_diy` 结尾的对象 |

### 1.2 不适用（豁免）

| 对象 | 理由 |
|---|---|
| 上游同步技能族 `lc-`(225) / `sci-`(151) / `ms-`(55) | 改名破坏上游整族覆盖升级；它们不是自建技能，本就不需要 `_diy` |
| 第三方独立技能（`tavily`、`pathclaw`、`finance-ops` 等） | 非自建，保持原名 |
| 生态保留名（`README.md`、`SKILL.md`、`META.md`、`LICENSE`、`index.html`、`requirements.txt`、`package.json` 等） | 被工具链、包管理器、宿主按固定名查找 |
| 依赖与构建目录（`node_modules/`、`site-packages/`、`dist/`、`build/`） | 第三方生成 |
| 代码内部标识符（变量、函数、CSS 类名） | 本规范只约束文件系统名称 |
| **历史存量名称** | 只报告、不改名（见 §5） |

### 1.3 目录层特别条款

- **不做**历史目录（2026-04 ~ 07，146 个）的批量改名迁移——收益仅观感，且目录名可能被会话索引引用（会话记录存于 SQLite），有断链风险。
- 系统自动创建的时间戳工作区：**不改名，只补 `META.md`**，首行写 `主题: <一句话>`。
- 仅对活跃项目目录改用 `<领域>-<主题>-<YYYYMMDD>`。

---

## 二、命名规则细节

三段式由 **领域前缀 + 用途中缀 + 主题 + 版本/状态** 构成，段间用 `-` 连接。

### 2.1 分类维度与依据

**采用"领域 × 用途"双轴。** 划分依据：**按"交付对象与合规链"划分领域，不按"技术手段"划分。**

不采用的维度及理由：

| 候选维度 | 结论 | 理由 |
|---|---|---|
| 按团队 | ✗ | 单人多智能体场景，无多团队归属，会造出空维度 |
| 按环境（dev/prod/test） | ✗ | 本地单机无部署环境；"环境"实际表达的是"草稿 vs 定稿"，属**状态**，下沉为后缀 |
| 按项目类型 | △ | 粒度太粗。"干细胞"和"专利"归档路径、合规链完全不同 |

**推论规则：脚本跟随它的服务对象，不跟随技术栈。** 例如影像处理脚本归 `med-`，不单设工具域；PDF OCR 服务于医学名词提取时归 `res-`，服务于合同解析时归 `ops-`。

### 2.2 领域前缀（N1，必选）

| 前缀 | 含义 | 适用范围 | 示例 |
|---|---|---|---|
| `med-` | 临床医学与神经科学 | 干细胞管线、脑卒中/ALS、影像处理、患者数据集、临床方案 | `med-761tel-ia-*`、`med-inpc-pet-suvr` |
| `res-` | 科研方法与文献产出 | 综述、Meta、文献检索与验证、实验设计、绘图方法 | `res-chictr-stroke-listing` |
| `ai-` | AI/大模型工具链与技能资产 | 技能开发发布、模型能力调研、dsh、GitHub 同步、自动化编排 | `ai-dsh-plugin-survey` |
| `grant-` | 基金与标书申报 | 齐鲁专项、国自然、尖兵计划、申报书与附件 | `grant-qilu-2026-94` |
| `pat-` | 专利与知识产权 | 交底书、权利要求、检索查新、法律状态核查 | `pat-cn121472147a` |
| `biz-` | 商业与投资决策 | 商业计划书、科学尽调、投资测算、房产投资调研 | `biz-qinhuangdao-housing` |
| `ops-` | 个人事务与运行维护 | 行程、通话转录、时间管理、环境排障、自动化配置 | `ops-harbin-trip-*` |

**边界易混点仲裁**

- 房产（自住兼投资）→ `biz-`；纯出行与生活事务 → `ops-`。**规则：涉金额决策与外部交易 → `biz-`；仅涉个人安排 → `ops-`。**
- 技能开发 vs 科研方法 → **规则：产出是工具 → `ai-`；产出是结论 → `res-`。**

### 2.3 用途中缀（N2，可选但推荐）

| 中缀 | 含义 | 典型产物 |
|---|---|---|
| `-lit-` | 文献检索与证据收集 | 检索清单、参考文献验证、ClinicalTrials 摘录 |
| `-rev-` | 综述与深度调研 | 调研报告、叙事综述、尽调结论 |
| `-dsgn-` | 方案与设计 | 技术路线、实验设计、标书路线、流程图 |
| `-data-` | 数据与数据集 | 影像数据、队列追踪表、结构化 JSON |
| `-fig-` | 图与视觉 | 机制示意图、路线图、封面、信息图 |
| `-doc-` | 正式成稿 | DOCX 终稿、通知书、合同、操作指引 |
| `-sld-` | 演示与汇报 | PPT、汇报页、验收汇报 |
| `-tool-` | 脚本与工具 | `.py` / `.mjs` / `.ps1` / `.bat` |
| `-raw-` | 原始素材 | 通话录音、扫描件、来源网页、原始响应 |

### 2.4 后缀（N3–N5）

| 类别 | 写法 | 规则 |
|---|---|---|
| 版本（N3） | `-v1` `-v2` | **必须带短横线**，禁止 `xxx2` / `xxx3` 裸接 |
| 状态（N4） | `-wip` `-pre` `-final` `-arch` | 缺省视为 `-wip` |
| 日期（N5） | `-YYYYMMDD`（8 位紧凑） | **仅**用于时间序列产物；禁止 `2026-08-20T01-45-01` 形式混入 |

### 2.5 分隔与自建标记（N6、N8、N9）

- **N6** 段间用 `-`；**禁止空格**。主题段内部可保留 `_` 以沿用中文命名习惯（`齐鲁制药专项_干细胞结合点` 合法）。
- **N9** 自建技能的 `_diy` 只能作为结尾标记，不得出现在名称中段。
- **N8** 自建技能若用 `-diy`（连字符）也视为自建标记，但建议统一为 `_diy`。

### 2.6 成对交付

md 与 docx **同名同基名**（`xxx.md` / `xxx.docx`）。已有 12 组实践跑通，禁止人工改名造成分叉。

### 2.7 保留项（不套命名模板）

`Claw`、`README.md`、`CITATION_AND_ATTRIBUTION.md`、`META.md`、`ALIAS.md` 等全大写约定文件，以及上游工具生成的固有产物名，一律保持原名。

---

## 三、澄清过程（强制前置）

**调用本技能为任何对象命名时，必须先完成澄清，再产出名称。** 完整规程见 `references/clarification.md`。

### 3.1 为什么必须澄清

名称的四个字段里，**没有一个能可靠地从名称或上下文推断出来**：

| 字段 | 可否推断 | 说明 |
|---|---|---|
| 领域前缀 | ❌ **不可靠** | 取决于"交付对象与合规链"。同一关键词在不同场景归不同域——PDF-OCR 服务于医学名词提取归 `res-`，服务于合同解析归 `ops-` |
| 用途中缀 | ❌ 不可 | 取决于处理阶段，名称里通常没有线索 |
| 主题 | ❌ **不可** | 必须由用户提供，工具不能编造 |
| 版本 / 状态 | ❌ 不可 | 取决于当前进度，只有用户知道 |

**跳过澄清直接命名 = 让工具替用户做语义判断，必然产出错名。** 校验器输出的"含领域推测"是**提问信号**，不是命名依据。

### 3.2 澄清规格

- 一次抛出**批量问题**，不做一问一答式碎片化来回
- 提问方式：用结构化选择题一次性呈现（每题选项 ≤5 个，**推荐项置首位并附理由**），而非散文式追问
- 单批次覆盖 **≥10 个信息点**（用决策树收敛，不必逐条问满）
- **每道选择题选项 ≤5 个**（领域有 7 类，故用决策树逐层缩小）
- 已明确的信息**不重复问**
- **总轮次上限 50 轮**；3 轮内无法收敛 → 降级为"给 2–3 个候选名 + 标注不确定字段"让用户直接挑

### 3.3 三批提问

**第 1 批：定位 + 领域收敛（10 个信息点）**

| 问 | 内容 | 选项（≤5） |
|---|---|---|
| 1 | 命名对象 | 文件／目录／自建技能／一批文件 |
| 2 | 新建还是改存量 | 新建／改存量 |
| 3 | 若改存量：是否被其他文件引用过 | 是／否／不确定 |
| 4 | 是否交付给外部机构或合作方 | 是／否 |
| 5 | 主要交付对象 | 自己日常／临床或科研同行／基金或专利机构／外部合作方或投资方 |
| 6 | 〔临床科研同行〕涉患者或临床，还是方法与证据 | 涉患者或临床／方法与证据 |
| 7 | 〔自己日常〕AI 工具链资产，还是生活事务 | AI 工具链资产／生活事务 |
| 8 | 〔基金或专利〕基金标书，还是专利事务 | 基金标书／专利事务 |
| 9 | 产物给人读，还是给机器或自己用 | 给人读／给机器或自己用 |
| 10 | 〔给人读〕调研结论／设计方案／成稿／演示；〔给机器〕脚本／数据／图／原始素材 | 各四选一 |

领域决策树（Q5 → Q6/Q7/Q8）：

```
自己日常使用 → AI 工具链资产 → ai- ｜ 生活事务 → ops-
临床或科研同行 → 涉患者或临床 → med- ｜ 方法与证据 → res-
基金或专利机构 → 基金标书 → grant- ｜ 专利事务 → pat-
外部合作方或投资方 → biz-
```

**领域仲裁规则**（用户答"不确定"时用）

| 情形 | 归属 | 规则 |
|---|---|---|
| 房产（自住兼投资） | `biz-` | 涉金额决策与外部交易 → `biz-`；仅涉个人安排 → `ops-` |
| 影像处理脚本 | `med-` | 脚本跟随服务对象，不跟随技术栈 |
| 技能开发 vs 科研方法 | `ai-` vs `res-` | 产出是工具 → `ai-`；产出是结论 → `res-` |

**第 2 批：主题与后缀（3–4 问）**

| 问 | 内容 |
|---|---|
| 1 | 主题用什么词（自由文本，可给 2–3 候选；支持中文，段内可用 `_`） |
| 2 | 版本：首版 `-v1`／第 N 版 `-vN`／不加 |
| 3 | 状态：草稿 `-wip`／待审 `-pre`／定稿 `-final`／归档 `-arch` |
| 4 | 是否时间序列产物（是 → 追加 `-YYYYMMDD`） |

〔若对象是技能〕额外一问：**自建（须以 `_diy` 结尾）还是上游/第三方（保持原名）？**

**第 3 批：确认（必做）**

给出完整候选名并**逐段回标来源**：

```
候选：grant-qilu-2026-94-dsgn-wip.md
  grant-        ← Q5 交付对象=基金机构，Q8=基金标书
  -dsgn-        ← Q9/Q10 给基金方的技术路线方案
  qilu-2026-94  ← 第2批主题词
  -wip          ← 第2批状态=草稿
```

若涉及**改名存量**，确认项还须包含：受影响路径全清单、引用风险判断、单批 ≤10 项说明、**需用户显式回复"确认执行"**。

### 3.4 澄清的边界

| 情形 | 处理 |
|---|---|
| 用户已一次性说清（如"给投资方写份商业计划书，初稿"） | 直接落 `biz-…-doc-wip`，**不重复问已明确项**，仅确认主题词与版本号 |
| 用户答"你定" | 按仲裁规则给出唯一解，**标注依据**并声明这是推定 |
| 纯校验已有名称 | 无需澄清，直接跑 `--name`。但结论含"建议改名"且涉及领域推测时 → **回头补问领域** |
| 一批文件批量命名 | 先问是否同一领域与用途；混合批次逐个确认 |
| 用户拒绝澄清 | 按已知信息出候选名，**明确标注哪些字段是推定** |

---

### 3.5 澄清完成的判据

以下**五项全部确定**，澄清才算完成，方可出名称：

1. **命名对象类型** —— 文件／目录／自建技能（决定套哪套规则）
2. **领域前缀** —— 7 类之一，且有明确判定依据（非推测）
3. **用途中缀** —— 9 类之一，或明确决定"省略该段"
4. **主题词** —— 由用户给出的字样
5. **版本与状态** —— `-vN` 与 `-wip/-pre/-final/-arch` 的取值

若涉及**改名存量**，额外需：受影响路径全清单 + 引用风险判断 + 用户显式"确认执行"。

任一项仍为推测状态时，**在候选名中显式标注**，不得默认为已确定。

---

## 四、校验逻辑

`scripts/check_name.py`，纯标准库零依赖。词表从 `references/scheme.md` 的 `vocab-*` 代码块解析，**扩充前缀只改文档、不改代码**。

**校验流程**

```
1. split_name()   切出 (stem, ext)，复合扩展名整体归 ext
2. 豁免判定       上游族 / 生态保留名 / 依赖目录 → 直接跳过
3. 逐条匹配       N1 领域前缀 → N2 用途中缀 → N3 版本号
                  → N4 状态   → N5 日期     → N6 空格
4. 技能附加判定   仅对 skills 下的自建技能：N7 结尾 _diy / N8 变体 / N9 中段
5. 生成建议名     按模板重排；临时件（`_` 前缀）建议移入 tmp/ 而非改名
```

**三种模式**

| 模式 | 用途 |
|---|---|
| `--name "xxx"` | 校验单个名称，回答"这个名字合不合规、该怎么改" |
| `--scan DIR` | 存量目录审计，只报告 |
| `--skills-root DIR` | 技能库审计：识别哪些是自建技能、哪些缺 `_diy` 标记 |

**用法**

```bash
# 有隔离环境时用其解释器绝对路径；否则直接用 python
PY="python"
SK="<技能目录>/scripts/check_name.py"

# 校验一个名称
"$PY" "$SK" --name "dsh插件装机量调研_速览版.md"
"$PY" "$SK" --name "med-761tel-ia-rev-v2.md"

# 校验目录，用 --out 写报告（Windows 上必须用 --out，不要用 > 重定向）
"$PY" "$SK" --scan "<待审计目录>" --out report.txt

# 审计技能库的自建标记
"$PY" "$SK" --skills-root "<技能库根目录>" --out skills-audit.txt
```

**退出码**：`0` 合规（可能有建议级）｜`1` 存在阻断级偏离｜`2` 参数错误。

**自建技能判定**（决定是否适用 N7）

```
名称以 _diy 或 -diy 结尾                     → 自建
SKILL.md frontmatter 有 agent_created: true   → 自建
名称前缀为 lc- / sci- / ms-                   → 非自建（上游族，直接跳过）
以上都不满足                                   → 非自建
```

---

## 五、违规处理

| 级别 | 适用对象 | 处理动作 |
|---|---|---|
| **P0 阻断** | 本次会话**新建**的文件/目录/技能 | 定名前修正，不写入；这是唯一"必须当场改"的情形 |
| **P1 警告 + 方案** | **存量**文件/目录 | 输出偏离清单与建议名，**等用户显式"确认执行"** |
| **P2 记录** | N4/N5/N8 建议级 | 只列不办，不阻断 |

### 安全红线

1. **绝不自动改名。** 校验器不含任何改名或删除 API，`--out` 只产出报告文本。
2. 改名前列出全部受影响路径并说明风险。
3. **单批 ≤10 项，改完复验；任一失败立即停止。**
4. 工作区顶层目录默认不进入改名计划（会话索引断链风险）。
5. 上游技能族、生态保留名一律只报告"豁免"，不进入改名计划。
6. 临时件（`_` 前缀）**建议移入 `tmp/` 而非改名**。

---

## 六、触发条件

### 6.1 显式触发

`/naming-convention_diy`；关键词：命名规范、命名方案、起名、给文件命名、命名校验、命名审计。

### 6.2 隐式触发（无需用户提出，自动执行）

**凡是要产出新名称或改名，一律先走 §3 澄清。** 下表各场景的"动作"均为澄清完成后的动作。

| 场景 | 动作 |
|---|---|
| **创建新技能** | 先问"自建还是上游/第三方"；自建则定名前套用 N7（**须以 `_diy` 结尾**，P0） |
| **新建交付文件或脚本** | 先澄清领域与用途，再套 N1 + N3 |
| **新建项目目录** | 先确认主题与起始日期，用 `<领域>-<主题>-<YYYYMMDD>` |
| **用户说"帮我命名/起个名"** | 直接进入 §3 澄清，**不得先给出名称再问** |
| **用户要求审计/整理命名** | 批量审计无需澄清，先报告、后处置（P1） |

---

## 七、预期行为

一次标准调用的固定序列。**前四步是澄清，不得跳过。**

1. **澄清定位** —— 判范围：逐对象给出 适用/豁免 及理由；确认新建还是改存量、是否被引用。
2. **澄清领域** —— 按"交付对象与合规链"决策树收敛到唯一领域前缀；跨域时按仲裁规则给出唯一解并说明取舍；**禁止用校验器的领域推测直接定案**。
3. **澄清用途** —— 从 9 个中缀中选出匹配项；无法归类则省略该段而非硬套。
4. **澄清主题与后缀** —— 拿到主题词、版本、状态；涉及改名时列出受影响路径并取得"确认执行"。
5. **出候选名并回标来源** —— 每个字段标注来自哪一问，供用户复核。
6. **形式校验** —— 跑 `check_name.py --name`，输出 `名称 | 违规规则 | 级别 | 建议名 | 是否需确认` 与退出码。
7. **处置** —— P0 当场改；P1 输出方案**等确认**；P2 仅记录。
8. **复验** —— 改名后重跑，确认退出码 0。

**不会被做的事**：**不跳过澄清直接给名称**；不自动改名；不因"像违规"就动豁免项；不把建议级当阻断级；不给代码内部标识符加后缀；**不把 `_diy` 加到普通文件或目录上**；不为通过校验改成无语义的占位名。

---

## 八、文件结构

```
naming-convention_diy/
├── SKILL.md                        # 本文件：范围、规则、澄清、校验、处置、触发
├── README.md                       # 仓库说明（对外）
├── references/
│   ├── clarification.md            # 澄清规程（提问清单、决策树、边界、终止条件）
│   ├── scheme.md                   # 完整方案与词表（vocab-* 块为校验器词源）
│   └── current-state.md            # 2026-09-17 现状扫描数据（方案的证据基础）
└── scripts/
    └── check_name.py               # 命名校验器（纯标准库）
```

## 九、已知限制

- 只校验**名称形式**，不判断语义恰当性（`med-aaa-rev-v1.md` 形式合规但无意义）
- 领域前缀的最终选择依赖语义判断，工具只能校验"是否属于合法词表"，不能校验"选得对不对"
- 澄清过程依赖用户配合；用户拒绝澄清时只能产出"标注了推定字段"的候选名
- 不改名、不修复，只报告
- 建议名不检测引用关系，执行前需人工确认该文件未被其他文件引用
- 存量审计会产生大量 P1 偏离（97% 目录无领域前缀），属预期结果，不应据此发起批量改名
